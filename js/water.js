'use strict';

class Product {
    constructor(x, type) {
        this.x = x;
        this.y = canvas.height - 200;
        this.radius = 20;
        /*this.type = type;
        this.color = (function() {
            if (this.type == 'water') {
                return 'aqua';
            } else {
                return 'white';
            }
        })();*/
    }
};